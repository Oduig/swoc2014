package com.gjos.scala.swoc

import com.gjos.scala.swoc.protocol.InitiateRequest2
import gos.bot.protocol.{Move, ProcessedMove, MoveRequest}

object JsonConverters {

  def createInitiateRequest(json: String): InitiateRequest2 = {
    ???
  }

  def createMoveRequest(json: String): MoveRequest = {
    ???
  }

  def createProcessedMove(json: String): ProcessedMove = {
    ???
  }

  def toJson(move: Move): String = {
    ???
  }

}
